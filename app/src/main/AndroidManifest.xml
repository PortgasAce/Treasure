<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.portgas.treasure.portgastreasure">

  <uses-permission
    android:name="android.permission.INTERNET"/>
  <uses-permission
    android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission
    android:name="android.permission.READ_EXTERNAL_STORAGE"/>
  <uses-permission
    android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:name="com.portgas.treasure.TreasureApplication"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <!--闪屏-->
    <activity
      android:name="com.portgas.treasure.SplashActivity"
      android:screenOrientation="portrait"
      android:theme="@style/SplashTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name="com.portgas.treasure.main.MainActivity">

    </activity>


    <!-- Demo:lingochamp/FileDownloader -->
    <activity android:name="com.portgas.treasure.download.DownloadMainActivity"/>
    <activity android:name="com.portgas.treasure.download.DynamicUrlActivity"/>
    <activity android:name="com.portgas.treasure.download.EncryptionActivity"/>
    <activity android:name="com.portgas.treasure.download.SingleTaskActivity"/>
    <!-- file download end -->

    <activity android:name="com.portgas.treasure.GlideActivity"/>

  </application>

</manifest>